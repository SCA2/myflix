- @categories.each do |category|
  %article.video_category
    %header
      %h3= link_to category.name, category
    .videos.row
      - category.videos.order(:title).each do |video|
        .video.col-sm-2
          = link_to video do
            = image_tag video.small_cover_url